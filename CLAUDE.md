# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`olivier385/Divers` — "Divers" is French for "Miscellaneous") is a catch-all repository for miscellaneous files and projects. It contains no source code, build configuration or tests yet.

## Structure

- `archivage-mails-sps/` — suivi du projet d'archivage des mails de la boîte secretariat.sps (macro Outlook `ArchivageSPS.bas`, script `pst_archive.py`, hors dépôt). Notes d'avancement datées (`point-AAAA-MM-JJ.md`) et guide de démarrage d'une session Claude Code sur un poste Windows (`guide-session-claude-code.md`), en français.

## Guidance

- There are no build, lint, or test commands to run at this time.
- Since this is a miscellaneous repository, content added here may be unrelated to other content. Treat each top-level directory or file as potentially independent rather than assuming a single unified project.
- When code or projects are added, update this file with the relevant commands (build, test, lint) and a description of the structure so future Claude Code instances can be productive quickly.
